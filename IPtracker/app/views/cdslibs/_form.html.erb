<%= form_for(@cdslib) do |f| %>
  <% if @cdslib.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cdslib.errors.count, "error") %> prohibited this cdslib from being saved:</h2>

      <ul>
      <% @cdslib.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
