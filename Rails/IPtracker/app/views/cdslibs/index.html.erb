<h1>Cadence Libraries</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @cdslibs.each do |cdslib| %>
      <tr>
        <td><%= link_to cdslib.name, cdslib %></td>
        <td><%= link_to 'Edit', edit_cdslib_path(cdslib) %></td>
        <td><%= link_to 'Destroy', cdslib, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cdslib', new_cdslib_path %>
